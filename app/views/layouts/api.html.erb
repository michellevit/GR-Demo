<!DOCTYPE html>
<html>
    <head>
    <meta charset="utf-8">
    <title><%= yield(:title) %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
    </head>
    <body>
        <div class="backend">
        <div class="container">
            <div class='main'>
                <%= yield %>
            </div>
        </div>
        </div>
    </body>
</html>