{
  "name": "heroku-deploy",
  "description": "Build scripts for Heroku",
  "engines": {
    "node": "20.10.0"
  },
  "scripts": {
    "clean": "rmdir /s /q public",
    "build": "npm install --prefix reactapp && npm run build --prefix reactapp",
    "deploy": "xcopy /E /Y reactapp\\build\\. public\\",
    "heroku-postbuild": "npm run clean && npm run build && npm run deploy"
  }
}